
	<div class="col-md-4">
		<% cache product do %>
		 <h1><%= product.name %></h1>
	   <%= link_to image_tag(product.image_url, class: "img-responsive"), product %>
	   <span class="price img-responsive"><%= (product.price).to_d %> CHF</span>
	  <% end %>
	</div>