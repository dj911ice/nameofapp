<!-- error handling -->
<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!-- form -->
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field(:name, size:36) %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area(:description, cols:35, rows:8) %>
  </div>
  <div class="field">
    <%= f.label :image_url %>
    <%= f.text_field(:image_url, size:36) %>
  </div>
  <div class="field">
    <%= f.label :colour %>
    <%= f.text_field(:colour, size:36) %>
  </div>
  <div class="field">
    <%= f.label :price %>
    <%= f.text_field(:price, size:8) %>
  </div>
  <br><br>
  <div class="actions">
    <%= f.submit(data: {confirm: "Do you wish to proceed with action?"}) %>
  </div>
<% end %>
