<div class="container">
	<br><br>
	<h1><%= @product.name %></h1>
	<br>
	<div class="row">		
		<div class="col-sm-4 col-md-6">
			<div class="product-show">
			<%= image_tag(@product.image_url, class: "img-show") %>
			<% if signed_in? && current_user.admin? %>
				<%= link_to 'Edit', edit_product_path(@product) %>
			<% else %>
			<% end %>
			<p><%= render partial: 'products/new_comment' %></p>
			</div>
		</div>
		<div class="col-sm-4 col-md-6">			
			<p class="description">
			<%= @product.description %>
			</p>
			<p class="price price-show"> Our Price: <%= (@product.price).to_d %> CHF</p>			
			<p class="rated" data-score="<%= @product.average_rating %>">Average Rating: </br>
			</p>
			<div class= "paybtn-stripe">
			<%= form_tag "/payments/create" do %>
				<%= render partial: "shared/stripe_checkout_button" %>
			<% end %>
			</div>
		</div>
	</div>
	<div class="row">
		<div class="col-sm-4 col-md-6">			
		</div>
		<div class="col-sm-4 col-md-6">
			<div class="apple_pagination">	
				<%= will_paginate @comments %>
			</div>
		</div>
	</div>	
	<%= render partial: 'products/comments' %>
	<% @product.highest_rating_comment %>				
</div>
